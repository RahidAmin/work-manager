import React from 'react'

export const metadata = {
    title: "All Tasks"
}

const ShowTasks = () => {
    return (
        <div>
            this is show tasks

        </div>
    )
}

export default ShowTasks
